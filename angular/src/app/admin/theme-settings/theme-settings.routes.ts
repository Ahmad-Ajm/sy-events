import { Routes } from '@angular/router';
import { ThemeSettingsComponent } from './theme-settings.component';

export const THEME_SETTINGS_ROUTES: Routes = [
  {
    path: '',
    component: ThemeSettingsComponent
  }
];

